README FILE FOR ForceBuild project

This is an empty project that VIX.Viewer.Service depends on to ensure all files are copied to its bin folder.
Visual Studio and MSBuild will not automatically build when files haven't changed, so this forces it.
